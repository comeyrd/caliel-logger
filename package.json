{
  "name": "caliel-logger",
  "version": "1.0.0",
  "description": "This is a way for alternant to not scan the qr code.",
  "main": "scan.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "https": "^1.0.0",
    "mysql2": "^3.9.7"
  }
}
